#include <stdio.h>

char* f(int x) {
	int loop = 0, r = 0, c = 0, cnt = 0;
	
	while(1):
		loop++;
		
		// loop가 홀수일 경우
		if( loop % 2 != 0)  {
			r = loop
			c = 1
			
			for(int i = 0; i < loop; i++) {
				cnt++;
				if(cnt == x) return static char 
			}
		}
}

main() {
	
}
